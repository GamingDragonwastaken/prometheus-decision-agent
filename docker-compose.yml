services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/prometheus.db
      GEMINI_MODEL: gemini-2.5-flash
      GEMINI_MODEL_NAME: gemini-2.5-flash
    restart: unless-stopped
    volumes:
      - ./data:/app/data
